{
  "name": "lista-de-eventos",
  "version": "1.0.0",
  "description": "Front End de uma aplicação de uma lista de eventos",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server --baseDir=dirt",
    "start": "concurrently \"npm run watch\" \"npm run server\"",
    "compile": "tsc",
    "watch": "tsc -w"
  },
  "author": "Gabriel Barbosa",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.2.2",
    "lite-server": "^2.6.1",
    "typescript": "^4.7.4"
  }
}
